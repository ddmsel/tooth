% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_tooth.R
\name{draw_tooth}
\alias{draw_tooth}
\title{Draw a single tooth as polygon geometry}
\usage{
draw_tooth(
  tooth_id,
  quadrant,
  tooth_num,
  is_upper,
  surface_values,
  value_col = "prop",
  x_offset = 0,
  y_offset = 0,
  tooth_size = 1,
  show_roots = TRUE,
  surfaces = c("buc", "lin", "mes", "dis", "occ", "rootb", "rootl", "rootm", "rootd"),
  display_label = NULL
)
}
\arguments{
\item{tooth_id}{Character label, e.g. \code{"ur1"}.}

\item{quadrant}{One of \code{"ur"}, \code{"ul"}, \code{"lr"}, \code{"ll"}.}

\item{tooth_num}{Numeric position within the quadrant.}

\item{is_upper}{Logical; TRUE for upper arch.}

\item{surface_values}{Data frame with \code{tooth_surface} and a value column.
Recognised surfaces: buc, lin, mes, dis, occ, rootb, rootl, rootm, rootd.}

\item{value_col}{Name of the value column in \code{surface_values}.}

\item{x_offset, y_offset}{Numeric offsets for positioning.}

\item{tooth_size}{Numeric side length of the crown square (default 1).}

\item{show_roots}{Logical; draw root surface bars (default TRUE).}

\item{surfaces}{Character vector of surfaces to include. Omit any you
don't want drawn.}

\item{display_label}{Optional custom label for the tooth number (e.g.
FDI notation \code{"11"} instead of \code{"ur1"}). When NULL, uses \code{tooth_id}.}
}
\value{
Named list of tibbles: crown, roots, labels, num_label, diags, outline.
}
\description{
Produces crown surface wedges (B/L/M/D/O) and up to four root surface
bars (RB/RL/RM/RD), plus labels, outlines, and diagonals — all as
data frames ready for ggplot2. Any surface can be excluded via the
\code{surfaces} argument.
}
