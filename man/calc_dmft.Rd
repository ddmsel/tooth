% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_dmft.R
\name{calc_dmft}
\alias{calc_dmft}
\title{Calculate DMFT / dmft index from surface- or tooth-level data}
\usage{
calc_dmft(
  data,
  format = c("long", "wide"),
  id = "record_id",
  group = NULL,
  strata = NULL,
  lesion_col = "lesion_code",
  activity_col = "act",
  filling_col = "filling_code",
  tooth_code_col = "code",
  root_lesion_col = NULL,
  root_surfaces = c("rootb", "rootl", "rootm", "rootd"),
  decayed_codes = c(3, 4, 5, 6),
  activity_codes = c(2),
  filled_codes = c(1, 2, 4, 5, 6, 7, 8),
  present_codes = c(2, 3, 4, 5, 6, 7, 8),
  missing_codes = c(1),
  root_decayed_codes = NULL,
  consider_activity = TRUE,
  consider_activity_coronal = NULL,
  consider_activity_root = NULL
)
}
\arguments{
\item{data}{Data frame. In \strong{long format}: one row per tooth-surface with
columns for id, tooth_num, tooth_surface, and clinical codes. In \strong{wide
format}: one row per person-tooth with surface codes in separate columns.}

\item{format}{\code{"long"} (default) or \code{"wide"}. See Details.}

\item{id}{Person-identifier column (default \code{"record_id"}).}

\item{group}{Optional grouping column(s) for repeated measures, e.g.
\code{"redcap_event_name"}. Character vector.}

\item{strata}{Optional stratification column(s), e.g. \code{"treatment"}. The
output will include these columns for downstream group summaries.}

\item{lesion_col, activity_col, filling_col, tooth_code_col}{Column names for
ICDAS codes (long format).}

\item{root_lesion_col}{Optional column for root surface lesion codes. When
provided, root caries (RDT) is calculated separately from coronal (DT).
Set to NULL to ignore root caries (default).}

\item{root_surfaces}{Character vector of surface names considered root
surfaces. Default \code{c("rootb","rootl","rootm","rootd")}.}

\item{decayed_codes, activity_codes, filled_codes, present_codes, missing_codes}{Numeric vectors defining clinical code categories. Defaults match ICDAS.
See the \emph{Diagnostic threshold} section for \code{decayed_codes}.}

\item{root_decayed_codes}{Numeric vector for root caries codes (default
same as \code{decayed_codes}).}

\item{consider_activity}{Logical; overall default for whether a lesion must
also carry an active-caries code (\code{activity_codes}) to be counted as
decayed. \code{TRUE} (default) requires activity; \code{FALSE} counts any lesion in
\code{decayed_codes} regardless of activity. Standard epidemiological D(3)MFT
counts all cavitated lesions irrespective of activity, so set \code{FALSE} to
match that convention. When \code{FALSE}, the activity column is not referenced
and need not be present.}

\item{consider_activity_coronal, consider_activity_root}{Optional logical
overrides for the coronal and root components. \code{NULL} (default) inherits
\code{consider_activity}. Use these to treat crown and root caries differently
(e.g. require activity coronally but not for roots, where activity is often
not assessed): \code{consider_activity_root = FALSE}.}
}
\value{
A tibble with one row per person (and group/strata) containing:
\describe{
\item{DT}{Decayed teeth (coronal)}
\item{FT}{Filled teeth (not decayed)}
\item{MT}{Missing teeth}
\item{DFT}{Decayed or filled teeth}
\item{DMFT}{Decayed + missing + filled teeth}
\item{num_teeth}{Count of teeth present}
\item{RDT}{Root-decayed teeth (only when \code{root_lesion_col} is set)}
\item{DT_yn, DFT_yn}{Binary indicators}
}
}
\description{
Computes Decayed, Missing, and Filled Teeth counts per person.
Handles both primary and permanent dentition. Can separate root
caries from coronal caries. Accepts long or wide format data.
}
\section{Diagnostic threshold}{

The default \code{decayed_codes = c(3, 4, 5, 6)} implements the \strong{D3 threshold}:
only ICDAS codes 3–6 (localised enamel breakdown through extensive
cavitation) are scored as decayed, while ICDAS 1–2 (non-cavitated initial
enamel lesions) are treated as sound. This D(3)MFT convention is the one
most commonly reported in caries epidemiology. To use the more sensitive D1
threshold that also counts early enamel lesions, pass
\code{decayed_codes = c(1, 2, 3, 4, 5, 6)}.
}

\section{Wide format}{

When \code{format = "wide"}, each row is one person (and optional group).
You must supply columns named like \verb{\{tooth\}_\{surface\}_\{code_type\}} or use
the \code{wide_cols} parameter. Alternatively, pass a \code{pivot_spec} — but the
easiest approach is to reshape to long with \code{\link[=pivot_to_long]{pivot_to_long()}} first.
}

