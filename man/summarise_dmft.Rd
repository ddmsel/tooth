% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_dmft.R
\name{summarise_dmft}
\alias{summarise_dmft}
\title{Summarise DMFT results by group}
\usage{
summarise_dmft(dmft_data, group_col, digits = 1)
}
\arguments{
\item{dmft_data}{Data frame returned by \code{\link[=calc_dmft]{calc_dmft()}}, optionally merged
with grouping variables (e.g. treatment arm, visit frequency).}

\item{group_col}{Column name to summarise by (e.g. \code{"treatment"}).}

\item{digits}{Number of decimal places for means (default 1).}
}
\value{
A list with two elements:
\describe{
\item{stats}{Summary data frame with one row per group, containing
\code{n}, \code{mean_teeth}, \code{mean_DT}, \code{mean_DMFT}. Column names match
what \code{\link[=build_odontogram]{build_odontogram()}} expects for \code{stats}.}
\item{raw}{The input data frame, for passing to \code{stats_raw} in
\code{\link[=build_odontogram]{build_odontogram()}}.}
}
}
\description{
Takes the output of \code{\link[=calc_dmft]{calc_dmft()}} and produces a summary table with
n, mean number of teeth, mean DT, mean DMFT, and optionally other
columns — ready to pass directly to \code{\link[=build_odontogram]{build_odontogram()}} via \code{stats}
and \code{stats_raw}.
}
\examples{
data(sim_exam)
dmft <- calc_dmft(sim_exam)
dmft$treatment <- ifelse(
  as.numeric(gsub("P", "", dmft$record_id)) <= 10, "SDF", "ART"
)
result <- summarise_dmft(dmft, "treatment")
result$stats
# Pass directly to build_odontogram:
# build_odontogram(d, strata = "treatment",
#   stats = result$stats, stats_raw = result$raw,
#   stats_test = "wilcox", stats_var = "DMFT")
}
