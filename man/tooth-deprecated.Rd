% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tooth-deprecated.R
\name{tooth-deprecated}
\alias{tooth-deprecated}
\alias{build_odontograph}
\title{Deprecated functions in tooth}
\usage{
build_odontograph(...)
}
\arguments{
\item{...}{Arguments passed on to \code{\link[=build_odontogram]{build_odontogram()}}.}
}
\value{
\code{build_odontograph()} returns the value of \code{\link[=build_odontogram]{build_odontogram()}}
(a \code{ggplot} object, or a list of \code{ggplot}s when \code{combine = FALSE}).
}
\description{
These functions are kept for backward compatibility but will be removed
in a future release. Each forwards to its renamed replacement.
}
\details{
\describe{
\item{\code{build_odontograph()}}{Deprecated in 0.5.0. The visualization is
now consistently called an \emph{odontogram} throughout the package. Use
\code{\link[=build_odontogram]{build_odontogram()}} instead. This wrapper forwards all arguments and
is fully equivalent, but emits a deprecation warning.}
}
}
\examples{
library(tibble)
d <- expand.grid(
  tooth_num = paste0(rep(c("ur", "ul", "lr", "ll"), each = 7), 1:7),
  tooth_surface = c("buc", "lin", "mes", "dis", "occ"),
  stringsAsFactors = FALSE
)
d$prop <- runif(nrow(d))
# Old name still works, with a warning:
\dontrun{
build_odontograph(d, teeth_per_quadrant = 7)
}
# Preferred:
build_odontogram(d, teeth_per_quadrant = 7)
}
\keyword{internal}
